const Apage = () => {
    return (
        <div>
            this page will have a nav bar because layout.tsx is in the same folder(homePage) which is
            called a route group. So all pages inside this route group will have the same layout.
        </div>
    );
}
export default Apage